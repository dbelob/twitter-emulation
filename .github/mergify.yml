pull_request_rules:
  - name: Automatic merge for Dependabot pull requests
    conditions:
      - author=dependabot[bot]
      - check-success=build
      - check-success=sonarcloud
    actions:
      merge:
        method: merge
